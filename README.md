# char2fonts

Find which fonts on your system contain specific characters.

Easies way to run is using `uv`:

```sh
uv run --script https://raw.githubusercontent.com/ContentFan/char2fonts/refs/heads/master/char2fonts.py <character>
```